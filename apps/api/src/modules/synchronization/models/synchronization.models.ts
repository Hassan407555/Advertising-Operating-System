/**
 * Domain models for synchronization results.
 * Kept as TypeScript interfaces under providers/interfaces —
 * re-exported here for the requested models/ folder layout.
 */
export type {
  SyncEntityResult,
  SyncEntityState,
  SyncIssue,
  SyncMetricsSnapshot,
  SyncRequest,
  SyncResult,
} from '../providers/interfaces/synchronization-provider.interface';
