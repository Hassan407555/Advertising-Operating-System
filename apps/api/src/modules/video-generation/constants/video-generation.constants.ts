export const VIDEO_GENERATION_PROVIDER = 'VIDEO_GENERATION_PROVIDER';

/** Target length for V1 product showcase videos (seconds). */
export const DEFAULT_VIDEO_DURATION_SECONDS = 8;

export const VIDEO_OUTPUT_WIDTH = 1080;
export const VIDEO_OUTPUT_HEIGHT = 1080;
export const VIDEO_FPS = 30;

/** Max product images used in the slideshow. */
export const MAX_VIDEO_SOURCE_IMAGES = 3;

/** Seconds each product image is on screen before the end card. */
export const SECONDS_PER_IMAGE = 2.2;

/** End card duration for headline + CTA. */
export const END_CARD_SECONDS = 2;
