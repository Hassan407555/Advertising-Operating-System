import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function AiCopyPage() {
  return (
    <PlaceholderPage
      title="AI Copy"
      description="AI Copy module implementation is intentionally deferred to a later phase."
    />
  );
}
