{
  "name": "advertising-operating-system",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.14.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@types/multer": "^2.2.0",
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "blurhash": "^2.0.5",
    "multer": "^2.2.0",
    "sharp": "^0.35.3"
  }
}
